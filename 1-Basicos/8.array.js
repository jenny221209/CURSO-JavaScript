// ARRAY
// Los array es una variable en la que se pueden almacenar varios datos o valores

/* 
SINTAXIS (como se realiza el array)

var variable = ["datoTexto", datoNumerico, DatoBoleano];

*/

var numero1 = 5;

var productoNC = ["camara fija interna", "Camara fija externa", "DVR - XVR", "video balum"];

console.log(productoNC);

// Imprimir en pantalla el registro 2
console.log (productoNC[2]);
